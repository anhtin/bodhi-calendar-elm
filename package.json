{
  "name": "bodhi-calendar",
  "version": "0.0.1",
  "description": "A calendar for buddhists to know when to eat vegetarian.",
  "homepage": "http://anhtin.github.io/bodhi-calendar",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-elm": "^0.6.1",
    "gulp-uglify": "^2.1.2"
  }
}
